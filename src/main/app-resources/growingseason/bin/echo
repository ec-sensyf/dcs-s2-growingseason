#!/usr/bin/python

import sys

def main(args):
    for n, arg in enumerate(args, start=1):
	print 'argument %d: <%s>' % (n, arg)

    for n, line in enumerate(sys.stdin, start=1):
	print 'input line %d: <%s>' % (n, line)
    
if __name__ == "__main__":
    main(sys.argv[1:])


